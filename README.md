# 🎨 luci-app-customlogo

一个专为 **OpenWrt 24.10 (LuCI2 架构)** 打造的轻量级 Web UI 美化插件。允许用户通过图形界面轻松上传并独立替换系统的 **网页图标 (Favicon)** 和 **导航栏 Logo**。

A lightweight Web UI customization plugin designed for **OpenWrt 24.10 (LuCI2 JS architecture)**. It allows users to easily upload and independently replace the **Web Icon (Favicon)** and **Navigation Bar Logo** via the Web UI.

---

## ✨ 核心特性 / Features

* 🚀 **全新架构兼容**：完全适配 OpenWrt 24.10 的 LuCI2 (客户端 JS 渲染) 架构，告别旧版 Lua CBI。
* 🖼️ **全面格式支持**：支持高清矢量图 `.svg` 以及标准透明图 `.png`。
* 🎛️ **极致物理解耦**：Favicon 与导航栏 Logo 独立设置、独立生效，底层逻辑互不干扰。
* 🪄 **智能 CSS 排版注入**：针对官方纯文本的 `Bootstrap` 主题，采用高级 Flexbox 与伪元素魔法，保留路由器名称文字的同时，将 Logo 以 28px 高度完美等比缩放并插入前方。
* 🛡️ **双重安全防线**：
* **前端实时校验**：引入 LuCI `fs` 模块，点击保存时异步校验物理文件存活状态，自动纠错与拦截“幽灵路径”。
* **无痕备份还原**：启用时自动备份系统原生文件，停用/卸载时瞬间恢复纯净状态，不残留任何注入的 CSS 与文件。


* 🎨 **多主题适配**：完美兼容官方原生 `Bootstrap` 主题以及广受欢迎的第三方 `Argon` 主题（含深色模式适配）。

## 📸 屏幕截图 / Screenshots

*(建议在这里放两张图 / Put your screenshots here)*

* **配置界面 (Settings UI)**
* **应用效果 (Result)**

## 📥 安装指南 / Installation

本项目已配置 **GitHub Actions** 自动编译，你可以直接下载最新编译好的 `.ipk` 安装包。

1. 进入本仓库的 [Actions 页面](https://www.google.com/search?q=../../actions)。
2. 点击最近一次带有绿色 `✅` 的 **"Build OpenWrt 24.10 IPK"** 任务。
3. 在页面底部的 **Artifacts** 区域，下载 `luci-app-customlogo-ipk` 压缩包。
4. 解压出 `.ipk` 文件，使用 WinSCP 或其他工具上传至路由器的 `/tmp` 目录。
5. 通过 SSH 连接到路由器，执行以下命令进行安装：

```bash
opkg update
opkg install /tmp/luci-app-customlogo_*.ipk

```

## 🕹️ 使用说明 / Usage

1. 登录 OpenWrt 路由器后台。
2. 导航至 **服务 (Services) -> 自定义 Logo (Custom Logo)**。
3. 勾选 **“启用自定义 Logo”**。
4. 在对应的选项中上传你的图片。
* **Favicon**：建议尺寸 `32x32` 或 `64x64` 的正方形图标。
* **Navbar Logo**：建议高度约 `40px` 左右的横向或方形图标（系统会自动限制为 28px 高度以保证排版美观）。


5. 点击 **保存并应用 (Save & Apply)**。
6. **⚠️ 极度重要**：由于浏览器对静态资源（尤其是 Favicon）存在极其顽固的缓存，应用后请务必按下 `Ctrl + F5` 强制刷新页面，或打开 **无痕模式/隐私模式** 窗口查看最新效果！

## 🛠️ 从源码编译 / Build from Source

如果你是一名开发者，想自行编译此插件：

```bash
# 1. 获取 OpenWrt SDK 并进入目录
# 2. 将本插件源码克隆至 package 目录
git clone https://github.com/crackerfly/luci-app-customlogo-repo.git package/luci-app-customlogo

# 3. 更新 feeds
./scripts/feeds update -a
./scripts/feeds install -a

# 4. 配置菜单项 (选择 LuCI -> Applications -> luci-app-customlogo)
make menuconfig

# 5. 编译
make package/luci-app-customlogo/compile V=s

```

编译产物位于 `bin/packages/*/base/` 目录下。

## 📝 许可证 / License

[MIT License](https://www.google.com/search?q=LICENSE)

---

**恭喜你，你的第一个高质量 OpenWrt 插件正式竣工了！** 如果未来有需要在前端表单里加点高级联动，或者适配其他的开源项目，随时呼叫我。
